export class MyFieldInfo {
  myAuthority: string;
  myField: string;
  myShop: string;
  constructor(myAuthority: string, myField: string, myShop: string) {
      this.myAuthority = myAuthority;
      this.myField = myField;
      this.myShop = myShop;
  }
}