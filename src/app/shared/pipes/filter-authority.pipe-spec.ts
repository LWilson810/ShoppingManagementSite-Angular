import {FilterAuthorityPipe} from './filter-authority.pipe';

describe('FilterAuthorityPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAuthorityPipe();
    expect(pipe).toBeTruthy();
  });
});
